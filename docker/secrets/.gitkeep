# Place Docker/Compose secret files here for production-style mounts.
# Local development uses root `.env` instead (never commit real secrets).
#
# Example (Compose secrets — optional, not required for local up):
#   jwt_access_secret.txt
#   jwt_refresh_secret.txt
#   data_encryption_key.txt
