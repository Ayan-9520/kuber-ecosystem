# KuberOne Redis — cache, sessions, queues, rate limiting
maxmemory 1gb
maxmemory-policy allkeys-lru
appendonly yes
appendfsync everysec
tcp-keepalive 300
timeout 0
databases 16

# Session DB: 0, Cache: 1, Queues: 2, Rate limit: 3
