# =============================================================================
# KuberOne — local environment overrides (optional)
# Docker stack uses committed .env.docker automatically — no copy required.
# For secrets/overrides:  cp .env.docker .env
# =============================================================================

# See .env.docker for full variable list and defaults.
