# OWASP ZAP baseline scan configuration (KuberOne)
# Usage: docker run -t ghcr.io/zaproxy/zaproxy:stable zap-baseline.py -t http://host:4000 -c zap-baseline.conf
10011	IGNORE	(Cookie No HttpOnly Flag)
10015	IGNORE	(Incomplete or No Cache-control and Pragma HTTP Header Set)
10021	IGNORE	(X-Content-Type-Options Header Missing)
10038	IGNORE	(Content Security Policy Header Not Set)
